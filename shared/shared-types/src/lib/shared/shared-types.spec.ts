import { sharedSharedTypes } from './shared/shared-types';

describe('sharedSharedTypes', () => {
  it('should work', () => {
    expect(sharedSharedTypes()).toEqual('shared/shared-types');
  });
});
